These files are copied from where bochs is installed.

Under GNU/Linux or Mac OS, it's unnecessary. 
But for Windows, we cannot use $BXSHARE to locate ROM images nor keymap files.

So here I want to unify the running script for different platform.

Finally, run.cmd and run.sh should be nearly same.